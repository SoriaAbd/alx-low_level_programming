#include "main.h"
#include <string.h>
/**
 * _strcmp - function that compares two strings.
 * @s1:string
 * @s2:string
 * Return:intger
 */

int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
